# Compatibility entry point for development/CI tooling.
# Product runtime dependencies are authoritative in setup.cfg and are installed
# by `pip install .` / wheel installation.
-r requirements-dev.txt
