name: AQR-012 validation patch

on:
  push:
    branches:
      - aqr-012-inventory-costs

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, 'validate schema 12 inventory') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: aqr-012-inventory-costs
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/aqr_012_validate_patch.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add aqorath/migrations.py
          git diff --cached --check
          git commit -m "fix: validate schema 12 inventory"
          git push origin HEAD:aqr-012-inventory-costs
