name: AQR-012 controller patch

on:
  push:
    branches:
      - aqr-012-inventory-costs

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, 'compose inventory presentation controller') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: aqr-012-inventory-costs
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/aqr_012_controller_patch.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add aqorath/presentation_controller.py
          git diff --cached --check
          git commit -m "feat: compose inventory presentation controller"
          git push origin HEAD:aqr-012-inventory-costs
